<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="actionBarStyle">@style/NoteActionBar</item>
    </style>
    
    <style name="NoteActionBar" parent="Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="elevation">0dp</item>
        <item name="displayOptions">showHome|useLogo</item>
    </style>
    
    <style name="TextLabel" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorHint">@color/light_text</item>

        <item name="colorControlNormal">@color/light_text</item>
        <item name="colorControlActivated">@color/light_text</item>
        <item name="colorControlHighlight">@color/light_text</item>
    </style>

    <style name="error_appearance" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/error_text</item>
    </style>



</resources>
